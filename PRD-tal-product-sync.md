# מסמך דרישות — ייבוא וסנכרון מוצרי טל בייבי ל-Shopify

> **מטרה:** להגדיר, שדה-אחר-שדה ופר-קטגוריה, איך מוצר מאתר **טל בייבי** (talbaby.co.il) מיובא ל-Shopify (חנות MaxBaby) ומסונכרן.
> **חלק ממערכת Supplier Product Sync.** אומת מול החנות החיה + על 8 מוצרי-בדיקה (קטגוריה-קטגוריה) + דף חי.
> **טל = ספק ריהוט לחדרי תינוקות.** אימות: 2026-07-20.

---

## 0. מקור הנתונים — **היברידי (API + דף HTML)** ⚠️
לטל **WooCommerce Store API ציבורי** (בלי WAF), אבל **המידות נמצאות רק בדף HTML** (שדה ACF), לא ב-API:
```
API:  GET https://www.talbaby.co.il/wp-json/wc/store/v1/products?per_page=100&page=N
דף:   GET <permalink>  →  לפרסר את acf-view "פרטים טכניים" (המידות!)
```
**חובה למשוך את שניהם:** ה-API לתיאור/מחיר/תמונות/וריאציות · הדף למידות.
- `formatted_dimensions` ב-API = **"אין מידע" לכל המוצרים** — המידות אינן שם! הן ב-`<acf-view__hzn-prtym-tknyym>` בדף (טאב "מידע נוסף" → "פרטים טכניים": `רוחב – 144 ס"מ · גובה – 200 · עומק – 60`).
- **הערה:** חלק מהמידות מופיעות בתוך ה-`description` (בעיקר שידות), חלק רק בדף ACF (ארונות/מיטות מעבר). למשוך משניהם.

---

## 1. מפתח זיהוי — ⚠️ מפתח חלש
- מק"ט (`sku` ↔ `variant.sku`) — **רק ~34% מהמוצרים** יש להם מק"ט. 0% חפיפה עם מק"טים ישנים בחנות.
- **מפתח משולב:** מק"ט (כשקיים) → נרמול-שם (מסיר דגם/צבע/טריקה שקטה) → שם-מוכל. **חובה לשמור `permalink` כ-`supplier_product_url`** = עוגן יציב לסנכרון.
- **94→ ה-50 הקיימים בחנות נבנו ידנית ומבולגן** (§3) — לבנות מחדש נקי מהשיטה כאן.

---

## 2. מיפוי שדות הליבה (POST `products.json`)
| שדה מקור | → שדה Shopify | כלל |
|---|---|---|
| `name` | `product.title` | as-is (**לפענח HTML entities** — `&#8211;`→`–`) |
| `sku` | `variant.sku` | מפתח (חלש) |
| `prices.price` | `variant.price` | להמיר מ-minor units (÷100). לייבא כמו שהוא |
| `images[]` | `product.images[]` | להוריד ולהעלות (לא hotlink) |
| `is_in_stock` / `stock_availability` | מלאי | §5 |
| `description` (API) + מידות (דף) | `custom.infoo` | §4 — **הליבה, עם ניקוי** |
| `name` + `short_description` | `body_html` | `"{name}"` + פסקת short_description |
| `categories[]` | `product_type` + אוסף | §6 |
| `permalink` | `supplier_product_url` | עוגן (מפתח חלש!) |
| *(קבוע)* | `vendor` = `Tal baby \| טל בייבי` · `status` = Draft (חדש) | |
| *(לפי סוג)* | `template_suffix` | §6 (furniture-beds / furniture-product-page) |
| וריאנטים | **מוצר בודד** | תוספות = הערה, לא וריאנט (§7) |

---

## 3. Metafields + סידור פר-קטגוריה
| metafield | שם | מקור |
|---|---|---|
| `custom.infoo` | מאפיינים | `description` (מנוקה) + **מידות** (מהדף) — §4 |
| `custom.securingg` | אחריות | **קבוע** (§8) |
| `custom.delivery` | משלוחים | **קבוע** (§8) |
| `custom.view_productss` | תיאור פריט | **קבוע:** "להוראות שימוש והרכבה: להורדה לחצו כאן" |
| `global.title_tag` / `description_tag` | SEO | ← name / description |

### ⚠️ הבעיה שתוקנה: הסידור ב-`infoo` שונה בין קטגוריות
התיאור הגולמי של טל **מערבב** מידע שונה בכל קטגוריה. §4 מגדיר ניקוי אחיד שמפריד:
| קטגוריה | מה יש בתיאור הגולמי | אחרי הניקוי (infoo) |
|---|---|---|
| שידות | תיאור + מידות + משלוח | תיאור + מידות |
| ארונות | שם + מחירון+גבולות+אחריות (דחוס) | שם + מידות (מהדף) |
| חדרי תינוקות | רכיבי הסט (מיטה+שידה) | רכיבי הסט |
| מיטות/מזרנים/לולים | הערת משלוח בלבד | תיאור (מ-short) |
| מיטות מעבר | משלוח+אחריות+מידות (מעורבב) | תיאור + מידות |

---

## 4. בניית `custom.infoo` — כללי הניקוי (rich_text) 🔑
`infoo` = `description` המומר ל-rich_text, **עם 4 כללי ניקוי**:
1. **לשמר bold + רשימות:** `<strong>`/`<b>` → `"bold":true` · `<ul><li>` → `list`/`list-item`. (טל מדגיש כותרות ופיצ'רים — לשמר.)
2. **להסיר boilerplate מוטמע:** לזרוק כל שורה שמכילה: `הובלה`, `משלוח`, `איסוף`, `אחריות`, `מחירון`, `גבולות האספק`, `ספק הריהוט`, `מוביל`, `המחיר אינו כולל`, `₪`, `ש"ח`, `בתיאום מראש`, `למתקין` — כי אלו קבועים בשדות נפרדים (§8).
3. **להסיר זבל:** shortcode שבור + CSS. לזרוק שורה עם: `Shortcode`, `#image`, `avf_view`, `width:`, `{`, `}`, `* ראש`, `הוראות הרכבה`. גם `<style>` ובלוקי `#image_NN {…}`.
4. **להוסיף מידות** (מהדף, סוף infoo): כותרת מודגשת "מידות:" + `רוחב – N ס"מ` / `גובה` / `עומק` / `אורך` (regex על "פרטים טכניים" בדף).

---

## 5. מלאי
| מקור | → פעולה |
|---|---|
| `is_in_stock=true` / `stock_availability` | `inventory_quantity` > 0, `inventory_management="shopify"` |
| out of stock | `inventory_quantity` = 0 (לא למחוק) |
> טל = **מלאי בוליאני** (זמין/לא). אין כמות מדויקת.

---

## 6. קטגוריות — קטגוריית-אתר → product_type → אוסף → template
### 6.1 מיפוי (אומת מול אוספי-התפריט "ריהוט")
| קטגוריה באתר טל | → product_type | → אוסף (id) | template |
|---|---|---|---|
| מיטות תינוק | מיטות תינוק | מיטות תינוק (480525025534) | **furniture-beds** |
| מיטות מעבר | מיטות מעבר | מיטות מעבר (477898440958) | furniture-beds |
| חדרי תינוקות | חדרי תינוקות | חדרי תינוקות (480525418750) | furniture-product-page |
| שידות לחדרי תינוקות | שידות החתלה | **שידות החתלה** (480525451518) | furniture-product-page |
| ארונות וכונניות | ארונות לחדרי ילדים | **ארונות לחדרי ילדים** (480529645822) | furniture-product-page |
| כורסאות הנקה | כורסאות הנקה | כורסאות הנקה (480540197118) | furniture-product-page |
| עריסות · לולים | לולים ועריסות | לולים ועריסות (480539672830) | furniture-beds |
| מזרנים ואביזרים | מזרונים לתינוק | מזרונים לתינוק (477899096318) | furniture-product-page |

> ⚠️ **תיקון קריטי (בוצע):** בעבר טל השתמש באוספים כפולים "שידות"/"ארונות לילדים" **שמחוץ לתפריט ריהוט**. אוחדו ל-"שידות החתלה"/"ארונות לחדרי ילדים" (שבתפריט), והכפולים נמחקו. **טל = סגל = שניר** — כולם באותם אוספים.

### 6.2 מסננים (לא מוצרים — להתעלם)
`שירות לקוחות` · `קריאות שירות` · `הזמנת סט ברגים ואביזרים` · `סט גלגלים למיטה` · `מגדלי למידה` (לא לייבא).

### 6.3 קטגוריה לא מזוהה → Draft + סימון (לא ליצור אוסף!)
קטגוריית-אתר שאינה בטבלה 6.1 → המוצר עולה **Draft + מסומן לבדיקה ידנית**. **לא ליצור אוסף אוטומטית** (מונע כפילויות/שגיאות). בעל החנות מחליט ידנית: לאיזה אוסף קיים לשייך, או ליצור חדש.

---

## 7. מוצרים עם תוספות (add-on) — **הערה, לא וריאנט**
מוצר `type=variable` עם attribute-תוספת (למשל "תוספת הדום" בכורסאות) → **מוצר בודד** + **הערה ב-infoo**:
```
תוספות אפשריות (תוספת הדום):
• הדום תואם בצבע ''טדי בר'' (+250 ש"ח)
```
(מוסיפים רק את התוספות עם עלות; מדלגים על "ללא תוספת". **לא** יוצרים וריאנט — קשה לגבות את הפרש-המחיר.)

---

## 8. קבועים (verbatim)
- **`custom.securingg` (אחריות):**
  ```
  אחריות רחבה 2 שנים
  כל מוצרי טל בייבי מסופקים עם אחריות רחבה של 2 שנים על פגמי חומר ומלאכות.
  ```
  > ⚠️ התיאור של טל לפעמים אומר אחרת (ארון שנה / מיטה 3) — **מתעלמים, הקבוע "2 שנים" גובר** (כמו במוצרים הקיימים).
- **`custom.delivery` (משלוחים):** boilerplate ריהוט ("שירות הובלה והרכבה לריהוט וחדרי תינוקות…", זהה לסגל/שניר).
- **`custom.view_productss`:** "להוראות שימוש והרכבה: / להורדה לחצו כאן".

---

## 9. שני תהליכים
- **ייבוא:** מוצר-אתר → Draft + §2-§8. סקופ: ~118 מוצרים באתר (פחות מסננים).
- **סנכרון:** לפי `supplier_product_url` (עוגן, כי מק"ט חלש) → מלאי + מחיר + לוג. תדירות 3-6ש'.

---

## 10. מה על המתכנת להשלים (Implementation TODO)
1. **מקור היברידי (חובה):** למשוך גם דף HTML לכל מוצר (ACF "פרטים טכניים" = מידות). §0/§4.
2. **מפתח חלש:** לבנות matcher (מק"ט→שם מנורמל→עוגן URL) + לשמור `supplier_product_url`. §1.
3. **ניקוי infoo:** 4 הכללים (bold/רשימות · boilerplate · זבל · מידות). §4.
4. **פענוח HTML entities** בכותרת (`&#8211;`). §2.
5. **תוספות → הערה** ב-infoo (לא וריאנט). §7.
6. **תמונות:** הורדה+העלאה ל-CDN.
7. **קטגוריות:** למפות לאוספי-התפריט הנכונים (§6.1), לא לכפולים.

---

## ⚠️ הסתייגות — היקף הבדיקה
- **הקטלוג:** נמשכו כל ~118 המוצרים (API) ✅.
- **המיפוי + הניקוי + המידות-מהדף:** נבדקו על **8 מוצרים (קטגוריה-קטגוריה)** — לא על כל הקטלוג. המתכן חייב QA מלא, במיוחד: המקור-ההיברידי (מידות), המפתח-החלש (עוגן), וניקוי הזבל על נתונים חיים.
- **המיפוי הוא הליבה המאומתת.**

---
*נבנה מאימות ישיר: Tal Store API + דף HTML ↔ MaxBaby Admin API. 2026-07-20. אומת פר-קטגוריה על 8 מוצרי-בדיקה חיים (Draft). category/collection ids ומבנה שדות = מפתחות יציבים.*
